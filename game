رائع! إذا كنت ترغب في **لعبة جوال تعمل عبر المتصفح** (أي لا تحتاج تحميلها من المتجر، بل تُلعب مباشرة على الويب)، فهذا خيار ممتاز ومجاني بالكامل للبدء — ويمكن لاحقًا تحويلها إلى تطبيق iOS أو أندرويد!

### ✅ الفائدة:
- تعمل على **جميع الأجهزة**: آيفون، أندرويد، حاسوب.
- لا تحتاج Xcode ولا ماك.
- يمكن بناؤها باستخدام **أدوات مجانية**.
- تُنشر بسهولة على الإنترنت.

---

## 🎮 فكرة: لعبة مزرعة بسيطة تعمل عبر المتصفح

سنبني لعبة اسمها مثلاً: **"مزرعتي الصغيرة"**  
- تزرع نباتات بالنقر.
- تجمع المحصول بعد وقت.
- تشتري بذورًا جديدة.
- تزيد مستواك.

---

## ✅ الأدوات المجانية التي ستحتاجها:

| الأداة | الوظيفة |
|-------|--------|
| **HTML + CSS + JavaScript** | لغات برمجة الويب (مجانية، تعمل في أي متصفح) |
| **Replit.com** | بيئة تطوير عبر الإنترنت (لا تحتاج تثبيت شيء) |
| **Canva أو OpenArt** | لتصميم صور بسيطة بالذكاء الاصطناعي |
| **FreeSound.org** | لأصوات مجانية (مثل نقر، حصاد) |
| **GitHub Pages أو Vercel** | لنشر اللعبة مجانًا على الإنترنت |

---

## 🚀 الخطوة 1: افتح Replit

1. اذهب إلى: [https://replit.com](https://replit.com)
2. سجّل حسابًا مجانيًا.
3. اضغط على **"Create Repl"**.
4. اختر **HTML, CSS, JS**.
5. سمّه مثلاً: `MyFarmGame`.

---

## 📂 هيكل المشروع البسيط

سيكون لديك 3 ملفات:

1. `index.html` — هيكل اللعبة  
2. `style.css` — التصميم (ألوان، أزرار، صور)  
3. `script.js` — المنطق (النقر، الزراعة، النقاط)

---

## 📄 1. `index.html`

```html
<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>مزرعتي الصغيرة</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div class="game-container">
    <h1>🌱 مزرعتي الصغيرة</h1>
    <p>النقاط: <span id="score">0</span></p>
    <p>المستوى: <span id="level">1</span></p>

    <div class="farm">
      <img src="https://openclipart.org/download/289728/1496555092.svg" 
           alt="أرض زراعية" class="field" />
      <button id="plantBtn">زرع بذرة 🌱</button>
    </div>

    <div class="shop">
      <h3>المتجر</h3>
      <button onclick="buyUpgrade()">شراء ترقية (50 نقطة)</button>
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>
```

---

## 🎨 2. `style.css`

```css
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  text-align: center;
  background-color: #e8f5e9;
  color: #2e7d32;
  margin: 0;
  padding: 20px;
}

the

.game-container {
  max-width: 500px;
  margin: auto;
  padding: 20px;
  border-radius: 15px;
  background: white;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
}

.farm {
  margin: 20px 0;
}

.field {
  width: 200px;
  height: 150px;
  margin-bottom: 10px;
}

#plantBtn {
  background-color: #4caf50;
  color: white;
  border: none;
  padding: 12px 24px;
  font-size: 18px;
  border-radius: 10px;
  cursor: pointer;
}

#plantBtn:hover {
  background-color: #388e3c;
}
```

---

## ⚙️ 3. `script.js`

```javascript
let score = 0;
let level = 1;
let upgradePurchased = false;

const scoreElement = document.getElementById("score");
const levelElement = document.getElementById("level");
const plantBtn = document.getElementById("plantBtn");

// زراعة بذرة
plantBtn.addEventListener("click", () => {
  score += 1;
  if (upgradePurchased) score += 1; // ترقية تضاعف النقاط
  scoreElement.textContent = score;

  // ترقية تلقائية للمستوى
  if (score >= level * 10 && level < 5) {
    level++;
    levelElement.textContent = level;
    alert(`🎉 تهانًا! ارتقيت إلى المستوى ${level}`);
  }

  // تأثير النقر
  plantBtn.textContent = "زرعت! 🌱";
  setTimeout(() => {
    plantBtn.textContent = "زرع بذرة 🌱";
  }, 300);
});

// شراء ترقية
function buyUpgrade() {
  if (score >= 50 && !upgradePurchased) {
    score -= 50;
    upgradePurchased = true;
    scoreElement.textContent = score;
    alert("✅ اشتريت ترقية: كل نقرة تعطي نقطتين!");
  } else if (upgradePurchased) {
    alert("لقد اشتريت هذه الترقية مسبقًا!");
  } else {
    alert("ليس لديك نقاط كافية!");
  }
}
```
